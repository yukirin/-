# 基礎からのベイズ統計学 4-9 (4)

# 初期値
x <- c(0.3, 0.2, 0.5)

# 遷移核
T = matrix(c(0.2, 0.2, 0.6, 0.1, 0.6, 0.3, 0.3, 0.5, 0.2), 3,  3, byrow=T)

for (i in 1:20) {
  print(sprintf("%d : %f", i, x), quote=F)
  x <- x %*% T
}